import "./storage.js";
import React from "react";
import ReactDOM from "react-dom/client";
import TradingJournalRoot from "./TradingJournal.tsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TradingJournalRoot />
  </React.StrictMode>
);
